- `QualityPrototype::level` is `uint32`, ruhroh. so, no negative nor fractional levels ...
  - can we iterate over literally every prototype in the game and reduce their stats appropriately? :P

- lmao enabling `draw_sprite_by_default` renders quality icons ON QUALITY ICONS. lol. think that's a bug.
- need to render the texutual name ("Slop") on null-quality items; and need to disable the textual name on "Fine"-quality items
